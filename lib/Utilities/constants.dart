import 'package:flutter/material.dart';

const titleTextStyle = TextStyle(
  color: Colors.white,
  fontSize: 28.0,
  fontFamily: 'Piedra',
);

const greetTextStyle = TextStyle(
  fontSize: 18.0,
);

/*String getstatus(int c) {
  if (c == 0 && itemList.length > 0) {
    return 'You have incomplete tasks, get to work champ';
  } else if (c == 0 && itemList.length == 0) {
    return 'Man, assign yourself some tasks';
  } else if (c != 0 && c / itemList.length < 0.50) {
    return 'You have way to go, keep going';
  } else if (c / itemList.length > 0.50 && c != itemList.length) {
    return 'Good going, you are almost done';
  } else {
    return 'You\'re the best. Excellent!';
  }
}*/
